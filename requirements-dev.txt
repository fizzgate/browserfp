# 采集与扩展验证用的可选依赖。**不装也能跑主门禁** —— 缺了对应那几条会显式
# SKIP（结果行里印出原因，汇总里单列计数），不是静默跳过、也不是 FAIL：
# 环境缺件不该表现成代码坏了。
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# ⚠ 这三个**未必装得上**，实测（2026-08-03，macOS + Python 3.10）：
#
#   aioquic     ✓ 1.3.0 装上即可用。QUIC 跨连接稳定性（oracle/h3probe.py → test_h3）
#
#   wreq        ✗ **要求 Python >= 3.11**，3.10 上 pip 直接找不到任何版本。
#                 采 Firefox 系列指纹用（oracle/wreqcollect.py）。
#
#   curl_cffi   ✗ 本机装得上（0.16.0）但 import 崩：
#                 symbol not found in flat namespace '_CFArrayAppendValue'
#                 —— macOS 上的 wheel 与环境里的 CoreFoundation 对不上。
#                 出 golden 向量、真实 HRR 端到端（test_match / test_live_handshake /
#                 test_echo_fingerprint / test_cf_discrimination + oracle/ 采集脚本）。
#
# 正因为「未必装得上」，它们才是可选的：写成硬依赖的话这个仓在不少机器上
# 连门禁都跑不起来，而那些门禁验的是**扩展面**，不是核心正确性。
aioquic
wreq          ; python_version >= "3.11"
curl_cffi
